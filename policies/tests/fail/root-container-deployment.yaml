apiVersion: apps/v1
kind: Deployment
metadata:
  name: root-container-deployment
  labels:
    cost-center: eng
    team: payments
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bad
  template:
    metadata:
      labels:
        app: bad
        cost-center: eng
        team: payments
    spec:
      containers:
        - name: app
          image: nginx:1.25.0
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
